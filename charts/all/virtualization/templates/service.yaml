apiVersion: v1
kind: Service
metadata:
  name: {{ include "virtualization.fullname" . }}
  labels:
    {{- include "virtualization.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "virtualization.selectorLabels" . | nindent 4 }}

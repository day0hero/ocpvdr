apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "virtualization.fullname" . }}-test-connection"
  labels:
    {{- include "virtualization.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "virtualization.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

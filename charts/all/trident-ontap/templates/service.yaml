apiVersion: v1
kind: Service
metadata:
  name: {{ include "trident-ontap.fullname" . }}
  labels:
    {{- include "trident-ontap.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "trident-ontap.selectorLabels" . | nindent 4 }}
